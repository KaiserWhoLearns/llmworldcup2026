# Core
pydantic>=2.6
python-dotenv>=1.0

# Provider SDKs (install only the ones you use; all are listed for convenience)
anthropic>=0.49        # Anthropic — Fable 5, Opus 4.8
openai>=1.40           # OpenAI — and Together AI (OpenAI-compatible endpoint)
google-genai>=0.3      # Google — Gemini
